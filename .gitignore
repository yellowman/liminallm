# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
.env
.venv
env/
venv/
ENV/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Testing
.pytest_cache/
.coverage
# Written by `--cov-report=xml`, which is what CI runs and what reproducing a
# CI failure locally therefore also runs. `.coverage` above was ignored and
# this was not, so the reproduction left a generated file looking like work.
coverage.xml
htmlcov/
.tox/
.nox/

# Mypy
.mypy_cache/

# Local development
*.log
.DS_Store

# Redis
dump.rdb
*.rdb

# Left behind only if the mutation driver is killed mid-run.
.mutation-in-progress
